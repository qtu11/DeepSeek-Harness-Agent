# Extension Logo Assets

Source logo files for the Chromium extension live here.

- `logo-transparent-640.png` is the transparent source used to generate the
  shipped extension icons in `public/icons/`.
- `logo-preview-1024.png` is the larger rendered logo preview with background.

Only the generated icon sizes referenced by `public/manifest.json` should ship
in the unpacked extension.
